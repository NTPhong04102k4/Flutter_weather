> Nguồn: https://code.claude.com/docs/en/whats-new/2026-w21.md
> Tài liệu Claude Code — bản dịch tiếng Việt

# Tuần 21 · 18–22 tháng 5, 2026

> Dùng auto mode trên gói Pro và với Sonnet 4.6, xem những skill, subagent, và server MCP nào đang chi phối giới hạn gói của bạn trong /usage, và review diff bằng lệnh mới /code-review.

<div className="digest-meta">
  <span>Phiên bản phát hành <a href="/docs/en/changelog#2-1-143">v2.1.143 → v2.1.149</a></span>
  <span>1 tính năng · 18–22 tháng 5</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Auto mode trên gói Pro</span>
    <span className="digest-feature-pill">CLI</span>
  </div>

  <p className="digest-feature-lede">Auto mode giờ đã có mặt trên gói Pro và hỗ trợ Sonnet 4.6 bên cạnh Opus. Nó thay thế các lời nhắc xin phép (permission prompt) bằng các kiểm tra an toàn chạy nền: những hành động thường lệ chạy mà không làm gián đoạn bạn, còn những hành động phá hủy hoặc đáng ngờ sẽ bị chặn và đưa ra cảnh báo.</p>

  <p className="digest-feature-try">Cập nhật Claude Code, rồi xoay vòng các chế độ bằng Shift+Tab; auto mode sẽ xuất hiện khi tài khoản của bạn đáp ứng các yêu cầu:</p>

  ```bash terminal theme={null}
  claude update
  ```

  <a className="digest-feature-link" href="/docs/en/permission-modes#eliminate-prompts-with-auto-mode">Auto mode</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">Những cải tiến khác</p>

  <div className="digest-wins-grid">
    <div><a href="/docs/en/costs#track-your-costs"><code>/usage</code></a> giờ hiển thị bảng phân tích theo từng hạng mục về những gì đang chi phối giới hạn gói của bạn, quy usage gần đây cho các skill, subagent, plugin, và từng server MCP</div>
    <div>"Extra usage" được đổi tên thành "usage credits" trên toàn bộ CLI, và <code>/extra-usage</code> giờ là <code>/usage-credits</code>. Tên cũ vẫn hoạt động.</div>
    <div>Lệnh mới <a href="/docs/en/code-review"><code>/code-review</code></a> báo cáo các lỗi về tính đúng đắn (correctness) ở một mức độ nỗ lực do bạn chọn, chẳng hạn <code>/code-review high</code>, và <code>--comment</code> đăng các phát hiện dưới dạng bình luận inline trên PR GitHub. <code>/simplify</code> vẫn giữ vai trò là một lượt review chỉ nhằm dọn dẹp (cleanup-only) riêng biệt.</div>
    <div>Các phiên chạy nền giờ xuất hiện trong <code>/resume</code> bên cạnh các phiên tương tác, được đánh dấu bằng <code>bg</code>, và các phiên được ghim bằng <code>Ctrl+T</code> trong <code>claude agents</code> vẫn được giữ sống khi rảnh (idle)</div>
    <div><code>claude agents --json</code> liệt kê các phiên đang hoạt động dưới dạng JSON để phục vụ scripting, chẳng hạn thanh trạng thái và bộ chọn phiên</div>
    <div>Công cụ PowerShell giờ được bật mặc định trên Windows cho người dùng Amazon Bedrock, Agent Platform của Google Cloud, và Microsoft Foundry; tắt bằng <code>CLAUDE\_CODE\_USE\_POWERSHELL\_TOOL=0</code></div>
    <div><code>claude plugin disable</code> giờ từ chối khi một plugin đang bật khác phụ thuộc vào plugin đích, và <code>claude plugin enable</code> buộc bật các phụ thuộc bắc cầu (transitive)</div>
    <div>Ô duyệt marketplace của <code>/plugin</code> hiển thị chi phí ngữ cảnh dự kiến, và các màn hình Discover và Browse liệt kê các command, agent, skill, hook, và server MCP/LSP của một plugin trước khi cài đặt</div>
    <div>Cài đặt mới <code>worktree.bgIsolation: "none"</code> cho phép các phiên chạy nền chỉnh sửa trực tiếp bản sao đang làm việc (working copy) mà không cần <code>EnterWorktree</code>, dành cho các repo mà worktree không khả thi</div>
    <div>Đầu ra Markdown giờ hiển thị các ô đánh dấu (checkbox) danh sách công việc theo chuẩn GFM, và ô chi tiết của <code>/diff</code> cuộn được bằng bàn phím</div>
    <div>Đầu vào JSON của status line giờ bao gồm thông tin repo GitHub và PR khi phát hiện được</div>
    <div>Enterprise: cài đặt được quản lý <code>allowAllClaudeAiMcps</code> nạp các connector MCP đám mây của claude.ai bên cạnh <code>managed-mcp.json</code></div>
  </div>
</div>

[Changelog đầy đủ cho v2.1.143–v2.1.149 →](/en/changelog#2-1-143)
