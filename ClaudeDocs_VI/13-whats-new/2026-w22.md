> Nguồn: https://code.claude.com/docs/en/whats-new/2026-w22.md
> Tài liệu Claude Code — bản dịch tiếng Việt

# Tuần 22 · 25–29 tháng 5, 2026

> Chạy Claude Code trên Claude Opus 4.8, điều phối các tác vụ lớn với dynamic workflows, phát hiện các vấn đề bảo mật với plugin security-guidance, và dùng fast mode trên Opus 4.8 với giá thấp hơn.

<div className="digest-meta">
  <span>Phiên bản phát hành <a href="/docs/en/changelog#2-1-150">v2.1.150 → v2.1.157</a></span>
  <span>4 tính năng · 25–29 tháng 5</span>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Claude Opus 4.8</span>
    <span className="digest-feature-pill">mô hình mới</span>
  </div>

  <p className="digest-feature-lede">Opus 4.8 giờ là mặc định trên các gói Max, Team Premium, Enterprise pay-as-you-go, và Anthropic API. Nó mặc định ở mức nỗ lực cao (high effort); dùng <code>/effort xhigh</code> cho các tác vụ khó hơn. Yêu cầu v2.1.154 trở lên.</p>

  <Frame>
    <video autoPlay muted loop playsInline className="w-full" src="https://mintcdn.com/claude-code/QsIrGXGFg6xd7joy/images/whats-new/opus-4-8.mp4?fit=max&auto=format&n=QsIrGXGFg6xd7joy&q=85&s=6ebcf5fe136467da2b254de1fe749ea7" data-path="images/whats-new/opus-4-8.mp4" />
  </Frame>

  <p className="digest-feature-try">Chuyển sang Opus 4.8 bằng tên, hoặc chọn nó từ bộ chọn mô hình:</p>

  ```text Claude Code theme={null}
  > /model claude-opus-4-8
  ```

  <a className="digest-feature-link" href="/docs/en/model-config#available-models">Cấu hình mô hình</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Dynamic workflows</span>
    <span className="digest-feature-pill">bản xem trước nghiên cứu (research preview)</span>
  </div>

  <p className="digest-feature-lede">Một workflow là một script điều phối mà Claude viết cho tác vụ của bạn và chạy trên nhiều subagent ở chế độ nền. Hãy dùng nó khi một tác vụ quá lớn để một cuộc hội thoại có thể điều phối: một cuộc kiểm toán trên toàn bộ codebase, một cuộc di trú lớn, một câu hỏi nghiên cứu cần kiểm chứng chéo. Quản lý các lần chạy bằng <code>/workflows</code>.</p>

  <Frame>
    <img className="w-full" src="https://mintcdn.com/claude-code/QsIrGXGFg6xd7joy/images/whats-new/dynamic-workflows.png?fit=max&auto=format&n=QsIrGXGFg6xd7joy&q=85&s=26671fa8607cec3453ed9753f821bd4f" alt="Claude Code on Opus 4.8 showing a Dynamic workflow requested indicator for a prompt that asks for a workflow to migrate every internal fetch() call" width="3840" height="2160" data-path="images/whats-new/dynamic-workflows.png" />
  </Frame>

  <p className="digest-feature-try">Mô tả tác vụ và yêu cầu một workflow:</p>

  ```text Claude Code theme={null}
  > create a workflow that migrates every internal fetch() call to the new HttpClient wrapper
  ```

  <a className="digest-feature-link" href="/docs/en/workflows">Dynamic workflows</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Plugin hướng dẫn bảo mật (security guidance)</span>
    <span className="digest-feature-pill">plugin</span>
  </div>

  <p className="digest-feature-lede">Plugin security-guidance review các thay đổi mã của Claude để tìm lỗ hổng và sửa chúng ngay trong cùng phiên. Nó chạy một lượt kiểm tra mẫu (pattern check) nhanh trên mỗi lần chỉnh sửa, một lượt review bằng mô hình ở cuối mỗi lượt, và một lượt review sâu hơn theo kiểu agentic khi commit hoặc push. Thêm các quy tắc dự án trong <code>.claude/claude-security-guidance.md</code>.</p>

  <Frame>
    <video autoPlay muted loop playsInline className="w-full" src="https://mintcdn.com/claude-code/QsIrGXGFg6xd7joy/images/whats-new/security-guidance.mp4?fit=max&auto=format&n=QsIrGXGFg6xd7joy&q=85&s=c91d865936411586f42b24c558bcdd1d" data-path="images/whats-new/security-guidance.mp4" />
  </Frame>

  <p className="digest-feature-try">Cài đặt nó từ marketplace chính thức của Anthropic:</p>

  ```text Claude Code theme={null}
  > /plugin install security-guidance@claude-plugins-official
  ```

  <p className="digest-feature-try">Sau đó kích hoạt nó trong phiên hiện tại:</p>

  ```text Claude Code theme={null}
  > /reload-plugins
  ```

  <a className="digest-feature-link" href="/docs/en/security-guidance">Plugin hướng dẫn bảo mật</a>
</div>

<div className="digest-feature">
  <div className="digest-feature-header">
    <span className="digest-feature-title">Fast mode trên Opus 4.8</span>
    <span className="digest-feature-pill">bản xem trước nghiên cứu (research preview)</span>
  </div>

  <p className="digest-feature-lede">Fast mode giờ mặc định là Opus 4.8 ở mức \$10/\$50 cho mỗi MTok: gấp 2 lần mức giá tiêu chuẩn để có tốc độ khoảng 2.5 lần. Opus 4.7 và 4.6 giữ nguyên ở mức \$30/\$150. Fast mode của Opus 4.6 đã bị ngưng dùng (deprecated).</p>

  <p className="digest-feature-try">Bật/tắt fast mode, giờ chạy trên Opus 4.8:</p>

  ```text Claude Code theme={null}
  > /fast
  ```

  <a className="digest-feature-link" href="/docs/en/fast-mode#understand-the-cost-tradeoff">Giá của Fast mode</a>
</div>

<div className="digest-wins">
  <p className="digest-wins-title">Những cải tiến khác</p>

  <div className="digest-wins-grid">
    <div>Trong <code>claude agents</code>, thêm tiền tố <code>!</code> vào một lệnh shell để chạy nó như một tác vụ nền mà bạn có thể gắn vào và tách ra; cũng có sẵn dưới dạng <code>claude --bg --exec 'pytest -x'</code></div>
    <div>Các plugin trong thư mục <code>.claude/skills</code> giờ được nạp tự động, không cần marketplace, và <code>claude plugin init \<name></code> dựng khung (scaffold) một plugin mới</div>
    <div>Lệnh mới <code>/reload-skills</code> quét lại các thư mục skill mà không cần khởi động lại, và các hook <code>SessionStart</code> có thể trả về <code>reloadSkills: true</code> để làm cho các skill chúng cài đặt trở nên khả dụng ngay trong cùng phiên</div>
    <div>Các skill và command có thể đặt <code>disallowed-tools</code> trong frontmatter để gỡ các công cụ khỏi mô hình khi skill đang hoạt động</div>
    <div>Sự kiện hook mới <code>MessageDisplay</code> cho phép các hook biến đổi hoặc ẩn văn bản tin nhắn của trợ lý (assistant) khi nó được hiển thị</div>
    <div>Claude Code giờ chuyển sang <code>--fallback-model</code> mà bạn đã cấu hình cho phần còn lại của phiên khi không tìm thấy mô hình chính, thay vì thất bại ở mọi request</div>
    <div>Các plugin có thể khai báo <code>defaultEnabled: false</code> trong <code>plugin.json</code> hoặc một mục marketplace, để chúng được cài đặt mà không bật lên cho đến khi bạn kích hoạt</div>
    <div>Chế độ Vim: <code>/</code> ở chế độ NORMAL mở tìm kiếm lịch sử ngược (reverse history search), khớp với vi-mode của Bash và Zsh</div>
    <div>Thực thi công cụ theo kiểu streaming giờ luôn được bật, kể cả khi tắt telemetry và trên Amazon Bedrock, Agent Platform của Google Cloud, và Microsoft Foundry</div>
    <div><code>←←</code> để mở agents view giờ hoạt động trên Amazon Bedrock, Agent Platform của Google Cloud, Microsoft Foundry, và cả khi tắt telemetry</div>
    <div>Claude trong Chrome: chọn trình duyệt đã kết nối nào để dùng qua <code>/chrome</code> → "Select browser…", hoặc ngay trong chat khi một hành động trình duyệt chạy với nhiều trình duyệt đã kết nối</div>
    <div><code>claude mcp list</code> và <code>claude mcp get</code> giờ hiển thị các server <code>.mcp.json</code> chưa được phê duyệt là đang chờ phê duyệt thay vì tự động phê duyệt và kết nối khi đầu ra được đưa qua pipe</div>
  </div>
</div>

[Changelog đầy đủ cho v2.1.150–v2.1.157 →](/en/changelog#2-1-150)
